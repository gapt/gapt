proof \psi proves A(x), -A(x), C(x) :- C(x), B(x), D(x)


    cut( B(x) ) ;
    weak R( D(x) ) ;

    ax(B(x) :- B(x)) ;


    cut( A(x) ) ;

    weakL( A(x) ) ;
    ax(C(x) :- C(x)) ;

	weakR( A(x) ) ;
	weakR( B(x) ) ;
	negL( A(x) ) ;
  	ax(A(x) :- A(x));
