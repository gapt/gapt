proof \psi proves A(x), -A(x), C(x) :- C(x), B(x), D(x) {


    cut  ( B(x) ) ;

    cut( A(x) ) ;

	weak right ( A(x) ) ;
	weak right ( B(x) ) ;
	neg left ( A(x) ) ;
  	axiom(A(x) :- A(x));

    weak left ( A(x) ) ;
    axiom(C(x) :- C(x)) ;


    weak right( D(x) ) ;
    axiom(B(x) :- B(x)) ;

}